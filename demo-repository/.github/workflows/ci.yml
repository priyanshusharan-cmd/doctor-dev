name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # DELIBERATE GAP: Node 16 used here but package.json engines requires >=18
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      # DELIBERATE GAP: tests are not run in CI
      # - name: Run tests
      #   run: npm test
